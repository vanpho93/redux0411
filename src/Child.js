import React, { Component } from 'react';

class Child extends Component {
    render() {
        return (
            <div>
                <button>Add</button>
                <button>Sub</button>
                <button>Reset</button>
            </div>
        );
    }
}

export default Child;
